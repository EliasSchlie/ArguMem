from typing import Optional

def addMemory(
        memoryContent: str,
        context: str,
        dir: str,
        timestamp: Optional[str] = None,
        ):
    """
    Adds a memory
    """
    pass